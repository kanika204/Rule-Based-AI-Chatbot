# Rule-Based-AI-Chatbot
A simple Rule-Based AI Chatbot built with Python that responds to predefined user inputs using a dictionary of command-response pairs. The chatbot utilizes Python's get() method for efficient response retrieval and runs in a continuous loop until the user enters an exit command.
